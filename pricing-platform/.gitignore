# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environment (dev/PoC/demo — never push real Aiven/Render/Vercel creds)
.env
*.env.local
.env.production
.env.demo

# Node
node_modules/
dist/
frontend/.vite/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# MCP config (local project setup)
.mcp.json

# Claude local config (statusline, settings, permissions — never on GitHub)
.claude/

# Logs
*.log

# Local object-storage fallback (used when S3_BUCKET_NAME is unset)
backend/var/
